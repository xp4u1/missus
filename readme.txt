missus
======

Start a local webserver that allows other devices
to upload files directly to your machine.

Key features:

- Lightweight & simple to use
- Compatible with every OS (with a webbrowser)
- Saves files directly to your current
  working directory

Caution:

- Uses unencrypted HTTP, not secure for
  sensitive data!
